name: CD

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    if: secrets.RENDER_DEPLOY_HOOK != ''
    steps:
      - run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
